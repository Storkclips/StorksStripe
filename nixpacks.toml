[phases.setup]
nixPkgs = ["nodejs_20", "python311", "yarn"]

[phases.install]
cmds = [
  "cd frontend && yarn install",
  "cd backend && pip install -r requirements.txt"
]

[phases.build]
cmds = [
  "cd frontend && yarn build"
]

[start]
cmd = "cd backend && uvicorn server:app --host 0.0.0.0 --port $PORT"